name: Cloud Reconnaissance Workflow
tasks:
  - id: list_s3
    name: List AWS S3 Buckets
    adapter: aws
    params:
      operation: s3_list_buckets
      region: us-east-1
    retries: 1
    timeout: 30
